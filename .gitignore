# ── Python ──────────────────────────────────────────────
__pycache__/
*.pyc
.pytest_cache/

# ── Herramienta local (no es parte del proyecto) ────────
.codewhale/

# ── Instrucciones locales para agentes (cada carpeta tiene la suya) ──
AGENTS.md

# ── Datos del servidor (privados y/o pesados) ───────────
worlds/
backups/
behavior_packs/
resource_packs/
development_behavior_packs/
development_resource_packs/
development_skin_packs/
world_templates/
data/
definitions/
config/

# ── Binarios de Mojang (EULA/licencia: no redistribuir) ─
bedrock_server.exe
bedrock_server.pdb
bedrock_server_symbols.pdb
bds_update.zip

# ── Config del servidor (contiene datos locales) ────────
server.properties
permissions.json
allowlist.json
whitelist.json
packetlimitconfig.json
profanity_filter.wlist
packet-statistics.txt

# ── Historiales/archivos previos (limpieza del repo) ────
archived/

# ── Herramientas externas / generados pesados ────────────
static_analysis_semgrep_1/
gui_frontend/node_modules/
