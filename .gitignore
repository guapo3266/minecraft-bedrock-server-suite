# ── Python ──────────────────────────────────────────────
__pycache__/
*.pyc
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.venv/
venv/

# ── Herramienta local (no es parte del proyecto) ────────
.codewhale/
.zcode/
.playwright-mcp/
whatsapp-web-qr.png

# ── Datos del servidor (privados y/o pesados) ───────────
worlds/
backups/
behavior_packs/
resource_packs/
development_behavior_packs/
development_resource_packs/
development_skin_packs/
world_templates/
data/
definitions/
config/

# ── Binarios de Mojang (EULA/licencia: no redistribuir) ─
bedrock_server.exe
bedrock_server.pdb
bedrock_server_symbols.pdb
bds_update.zip
# Staging/resguardos transitorios del updater: si un crash los deja, no deben
# aparecer en git status (el lifespan los recupera al siguiente arranque).
bds_update_staging/
bds_update_prev_*/
# Doc oficial que viaja en el zip de BDS: no se redistribuye en el repo
# (el archivo local se conserva; el disclaimer del README lo exige).
bedrock_server_how_to.html

# ── Config del servidor (contiene datos locales) ────────
server.properties
permissions.json
allowlist.json
whitelist.json
packetlimitconfig.json
profanity_filter.wlist
packet-statistics.txt
# release-notes.txt: artefacto del zip de Mojang, se sobre escribe en cada
# update_bds (_apply_staged_update no lo preserva). Dato de instalacion.
release-notes.txt

# ── Historiales/archivos previos (limpieza del repo) ────
archived/

# ── Herramientas externas / generados pesados ────────────
static_analysis_semgrep_*/
gui_frontend/node_modules/
tools/gui_e2e_home.png
